---
# Monitoring Secrets for Grafana and Prometheus
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-secret
  namespace: monitoring
  labels:
    app: grafana
    component: monitoring
type: Opaque
data:
  # admin-password: admin123 (base64 encoded)
  # ⚠️  WARNING: This is a DEMO password - CHANGE THIS IN PRODUCTION
  # To generate: echo -n "your-secure-password" | base64
  admin-password: YWRtaW4xMjM=