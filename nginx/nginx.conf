# Define upstream API backend
upstream api_backend {
    server api:8000;
    keepalive 32;
}

# HTTP server block - Redirect to HTTPS
server {
    listen 8080;
    server_name localhost;

    # Security headers
    add_header X-Frame-Options DENY always;
    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Health check endpoints allowed over HTTP for load balancers/monitoring
    location /nginx-health {
        access_log off;
        return 200 "nginx healthy (redirecting to HTTPS)\n";
        add_header Content-Type text/plain;
    }

    location /health {
        proxy_pass http://api_backend/health;
        access_log off;
    }

    # Nginx stub_status for prometheus exporter (exact match to avoid redirect)
    location = /stub_status {
        stub_status;
        access_log off;
        # Allow localhost
        allow 127.0.0.1;
        allow ::1;
        # Allow Kubernetes pod network (10.0.0.0/8 covers most K8s installations)
        allow 10.0.0.0/8;
        # Allow Docker network
        allow 172.16.0.0/12;
        deny all;
    }

    # Redirect all other HTTP traffic to HTTPS
    # For Docker Compose staging environment, use port 30443
    location / {
        # Check if we're in a Docker Compose environment and use the correct external port
        if ($http_host ~* "localhost:30080") {
            return 301 https://localhost:30443$request_uri;
        }
        # Default redirect for other environments
        return 301 https://$host$request_uri;
    }
}

# HTTPS server block (TLS termination)
server {
    listen 8443 ssl;
    http2 on;
    server_name localhost;

    # SSL Certificate configuration
    # Docker Compose deployment - uses self-signed certificates
    # For Kubernetes deployments, see kubernetes/configmaps.yaml which uses tls.crt/tls.key
    ssl_certificate /etc/nginx/ssl/nginx-selfsigned.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx-selfsigned.key;

    # SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:50m;
    ssl_stapling off; # Self-signed certificates don't support OCSP stapling

    # Include common configuration
    include /etc/nginx/conf.d/common-config.conf;

    # Nginx stub_status for prometheus exporter (also on HTTPS for flexibility)
    location = /stub_status {
        stub_status;
        access_log off;
        # Allow localhost
        allow 127.0.0.1;
        allow ::1;
        # Allow Kubernetes pod network
        allow 10.0.0.0/8;
        # Allow Docker network
        allow 172.16.0.0/12;
        deny all;
    }

    # Nginx health check endpoint (HTTPS)
    location /nginx-health {
        access_log off;
        return 200 "nginx healthy (https)\n";
        add_header Content-Type text/plain;
    }

    # API health check (HTTPS)
    location /health {
        proxy_pass http://api_backend/health;
        access_log off;
    }

    # API routes with /api prefix (HTTPS)
    location /api/ {
        rewrite ^/api(/.*)$ $1 break;
        proxy_pass http://api_backend;
        proxy_read_timeout 60s;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        
        # CORS headers for API
        add_header Access-Control-Allow-Origin * always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept, Authorization" always;
        
        if ($request_method = 'OPTIONS') {
            return 204;
        }
    }

    # Serve static HTML files, fallback to API for dynamic routes (HTTPS)
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ @api;
    }

    # Fallback to API backend for non-static routes
    location @api {
        proxy_pass http://api_backend;
        proxy_read_timeout 60s;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
    }

    # Static files (HTTPS)
    location /static/ {
        alias /var/www/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri $uri/ =404;
    }

    # Favicon (HTTPS)
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }

    # Robots.txt (HTTPS)
    location = /robots.txt {
        log_not_found off;
        access_log off;
    }

    # Error pages (HTTPS)
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;

    location = /50x.html {
        root /usr/share/nginx/html;
    }

    location = /404.html {
        root /usr/share/nginx/html;
    }
}