# SSL Directory
# This directory will contain SSL certificates and keys

# Files that will be generated:
# - nginx-selfsigned.crt (SSL certificate)
# - nginx-selfsigned.key (Private key)  
# - dhparam.pem (Diffie-Hellman parameters)
# - ssl-params.conf (SSL configuration parameters)

# Note: These files are generated automatically by the generate-ssl.sh script
# Do not commit actual certificates to version control!